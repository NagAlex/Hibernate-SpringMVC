# Hibernate-SpringMVC
Using Hibernate framework and Spring MVC in building web application

Application allows to create, edit and delete groups and users of those groups. Groups and users are both parsing to (fetching from) database (MySQL) by meaning of Hibernate framework.
